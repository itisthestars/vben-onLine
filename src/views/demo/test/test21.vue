<template>
  <div id="think-demo-content" style="height: 50vh; overflow: auto" ref="talkContent">
    111111111111111 // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 //
    通常意味着用户向上滚动了 // if (element.scrollTop + element.clientHeight < element.scrollHeight
    - 2) { // // 5px 的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; // 111111111111111 //
    如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
    // 如果当前滚动位置小于最大滚动高度，说明用户没有在最底部 // 通常意味着用户向上滚动了 // if
    (element.scrollTop + element.clientHeight < element.scrollHeight - 2) { // // 5px
    的容差，避免因滚动条宽度等微小差异误判 // hasScrolledUp.value = true; //
    console.log('检测到用户向上滚动，停止自动滚动到底部'); // } else if(element.scrollTop +
    element.clientHeight == element.scrollHeight ) { // hasScrolledUp.value = false; //
    console.log('检测到用户向下滚动到底部，继续自动滚动到底部'); // // scrollToBottom(false); // }
  </div>
</template>
<script setup>
  import { ref, onMounted } from 'vue';
  const talkContent = ref(null);
  const hasScrolledUp = ref(false);
  // const lastScrollTop = ref(0);
  const scroolChange = () => {
    const element = talkContent.value;
    // if (!element) return;
    const currentScroollTop = element.scrollTop;
    // console.log('lastScrollTop', lastScrollTop.value);
    console.log('currentScroollTop', currentScroollTop);
  };

  onMounted(() => {
    talkContent.value.addEventListener('scroll', scroolChange);
  });
</script>

<style lang="scss" scoped></style>
